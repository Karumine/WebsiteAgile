# ----------------------------------------------------
# Development Environment (Local Dev Server - Mode: dev)
# ----------------------------------------------------
# Uses relative /api/v1 so Vite dev proxy in vite.config.ts
# forwards requests to https://api.tunjai.in.th without CORS issues.
VITE_API_URL=/api/v1
VITE_APP_ENV=development
